# Geliştirme bağımlılıkları (test, biçim ve tip denetimi).
# Dağıtılan uygulamaya dahil DEĞİLDİR.
#
# Kurulum:
#     pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
pytest-cov>=6.0,<8.0
ruff>=0.8,<1.0
mypy>=1.13,<2.0
types-beautifulsoup4
